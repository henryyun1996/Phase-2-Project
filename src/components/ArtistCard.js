

export default function ArtistCard({ artist }) {
    return (
       <div className="ui four wide column image">
        <h4>{artist.name}</h4>
        <h4>{artist.bio}</h4>
        <h2>testingg</h2>
       </div>
    )
}